[package]
name = "pfs-wasm-ffi"
version = "0.1.0"
edition = "2021"
authors = ["PFS Contributors"]
description = "Safe Rust SDK for building PFS WASM plugins"
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
crate-type = ["rlib"]
